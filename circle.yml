# The deployment section should be excluded until
# it is explicitly included in module 5
# trigger did not work so added a new trigger in circle ci settings
deployment:
  dockerhub:
    branch: master
    commands:
      - $DOCKER_HUB_TRIGGER

test:
  override:
    - mocha
